# SV Medicare Institute — deploys ascension-app (static export)
# For Git deploys: set Base directory = ascension-app in Netlify UI

[build]
  base = "ascension-app"
  command = "npm run build"
  publish = "ascension-app/out"

[build.environment]
  NODE_VERSION = "20"
