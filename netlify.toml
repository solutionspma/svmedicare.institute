# SV Medicare Institute — Ascension Mode (dark cinematic theme)
# ONLY ascension-app deploys. Do not use netlify-site or course-in-a-box.

[build]
  base = "ascension-app"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
