. Do I also need to update them on the example ENV# SV Medicare Institute — Ascension app
# ----------------------------------------------------------------
# Local dev: copy this file to `.env.local` and fill in values.
# `.env.local` is gitignored. Do not commit real secrets.
#
# Production (Netlify): Site configuration → Environment variables
# Use the same names for serverless functions (no NEXT_PUBLIC_* required
# for OpenAI — only OPENAI_API_KEY on the server).
# ----------------------------------------------------------------

Supernatural supernatural goddamnit# OpenAI — realtime voice + call analysis (Netlify functions only; never exposed to browser)
OPENAI_API_KEY=

# Optional — Realtime session model (default gpt-realtime). Try gpt-4o-realtime-preview if your org requires it.
OPENAI_REALTIME_MODEL=REDACTED_OPENAI_KEY

# Supabase — optional; required if you use auth / DB from the app
NEXT_PUBLIC_SUPABASE_URL=https://supjsnjsbtxsehmsjicv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_eNoMLuT2zrRgSAq9u0zQ6w_vefKFFb_


# Credential signing — issueCredential / verifyCredential functions
# Use a long random string in production (not the default dev fallback).
CREDENTIAL_SIGNING_SECRET=

# Optional — only for scripts/fetch-pexels-images.js
PEXELS_API_KEY=34bnCdunaiGUOBl9tVdJddI77uK4FfRxZNoZ2FkRLQWBc26wUaVTmQ51
Hello this is Jason recovery family financial services how may I help you hello this is Jason with cover family financial services how may I help you Mary hello your score is 62